package org.zlab.upfuzz;

public interface TestSequence {
    /**
     * mutate()
     * - Need to do all level mutation, including the command pool
     *
     * serialize/deserialize()
     * - A high level ser/de func.
     * - Will call all the following ser/de func.
     */
}
